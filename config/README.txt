This directory is for files which we'd like to keep under version
control, but don't fit anywere else. Mostly these are server-side
scripts and config. This will eventually be replaced with
elastic-beanstalk config.
